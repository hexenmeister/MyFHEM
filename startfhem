#!/bin/sh

home=/opt/fhem

cd $home

# Start watchdog
./runwatchdog.sh

cnt=`ps -ef | grep "fhem.pl" | grep -v grep | wc -l`
if [ "$cnt" -eq "0" ] ; then
  echo "Starting fhem..."
  perl fhem.pl fhem.cfg
else
  echo "fhem is allready running"
fi
